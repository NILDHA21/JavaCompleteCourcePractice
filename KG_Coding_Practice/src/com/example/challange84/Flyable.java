package com.example.challange84;

/*84. Create an interface Flyable with an abstract method fly().
 * Create an abstract class Bird that implements Flyable.
 * Implement a subclass Eagle that extends Bird.
 * Provide an implementation for the fly() method.*/

public interface Flyable {

	void fly();
}
